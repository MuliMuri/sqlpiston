Installation
============

SQLPiston works with Python 3.9+ on Linux / macOS / Windows.

For now, you can clone the repo and set up the environment.

.. code-block::

    git clone https://github.com/MuliMuri/sqlpiston.git
    cd sqlpiston
    pip install -e .
